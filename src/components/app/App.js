import React from 'react';
import { hot } from 'react-hot-loader';
import ListContainer from '../list-container/list-container';

const App = () => <ListContainer />;


export default hot(module)(App);